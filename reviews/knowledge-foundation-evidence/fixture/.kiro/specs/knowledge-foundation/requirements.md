# Knowledge foundation requirements

The accepted plan requires projectctl knowledge validate to run deterministically and locally without a model turn or an okn subprocess.
The replacement search must use Kiro ACP and return independently verifiable source quotations within a host-enforced returned-context budget.
The current implementation must be inspected before claiming these requirements are implemented.
