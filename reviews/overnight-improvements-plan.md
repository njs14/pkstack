# Overnight improvement plan

Baseline: `96210242ff59db728282527e241777fb965d58c1` in the isolated
`aqua-chasm` worktree. Implementation and final verification remain pending.

## Required outcomes

1. Adapt Matt Pocock's upstream `wayfinder` into an installed Kiro CLI v3 skill.
   Preserve destination agreement, decision tickets, dependencies, fog, bounded
   frontier selection, evidence-backed resolution, and resumption. Hand the cleared
   map to native Kiro planning before implementation. Support GitHub Cloud only;
   exclude alternate trackers, local Markdown tracker fallbacks, and self-hosted
   hosts. Publishing a real map requires authority for that repository.
2. Rename the existing `pkstack` skill to `poteto-kiro-mode`, including its directory,
   frontmatter, active invocation examples, discovery, routing, and metadata.
   Preserve the PKStack product/package identity and the `pkstack` custom agent.
   In particular, `/agent swap pkstack` remains an agent command.
3. Refresh current artwork using the requested image-generation 2.5 if available.
   Keep the friendly scholarly ghost family, book, knowledge tree, and palette.
   Replace the Pocock figure's glasses/TS badge with an abstract teaching or
   craftsmanship motif; use a subtle cooperation/control-loop motif for HumanLayer.
   Preserve positive attribution in prose without suggesting endorsement. Inspect
   the final logo at README size and the Power image at thumbnail size. Record the
   generation method, prompt, dimensions, checksums, and consuming references.
4. Finish with a fresh upstream inventory, review every changed imported path,
   integrate relevant semantics, and accept updates through the existing proposal
   and provenance mechanism. Account for every configured source in the final
   aggregate check. Report transport or review failures as unresolved.

## Order and work ownership

- Inspect the other Pocock thread's final candidate before integrating its changes.
  On initial inspection the main checkout has uncommitted imports, router changes,
  upstream machinery changes, and new catalog metadata. Do not copy this partial
  candidate wholesale or edit that checkout. Its deferred Wayfinder entry is the
  integration point for this work.
- Work on artwork independently while the other candidate develops. The current
  tool exposes no image model selector. The user clarified that the latest
  available built-in image capability is intended. The refreshed README crest is
  generated, visually inspected, and saved; Power thumbnail work remains pending.
- Read the actual upstream Wayfinder bundle and its GitHub recipes; define the
  Kiro handoffs and GitHub-only operations before implementation. Reuse the
  installed grilling/domain-modeling methods and reviewed incoming helpers.
- Implement the skill rename against a stable source layout. Review occurrences
  by role instead of globally replacing `pkstack`; historical evidence and agent,
  package, executable, and upstream identities have different owners.
- Run focused routing, packaging, and bootstrap checks after these changes. Refresh
  generated assets through the reviewed setup workflow.
- Perform the final upstream review after feature and artwork work, followed by
  the full locked local gate and a requirement-by-requirement completion audit.

## Evidence and efficiency

- Read upstream sources once at immutable commits. Keep complete source inventory
  and per-path dispositions while using bounded review batches for reading.
- One writer owns shared manifests and generated-state refresh. Avoid repeated
  broad gates until the integrated candidate is ready.
- Exercise meaningful scenarios: fresh GitHub prerequisites, existing-map resume,
  blocked frontier, unsupported host, missing authorization, native Plan/Spec
  handoff, and old-versus-new skill discovery. Structural routing checks alone
  do not prove live Kiro behavior.
- Initial aggregate check returned `GitHub API returned HTTP 403`; it provides no
  current upstream verdict. Investigate authenticated read access and retry without
  exposing credentials. Keep hosted workflow activation outside this task.
- Completion requires the installed skill, verified rename, refreshed consumed
  images, reviewed source updates, generated parity, and passing required checks.
  A plan or a selected-source acceptance alone is insufficient.
