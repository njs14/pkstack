# Release-status compatibility link

The current release ledger is
[`release-status.md`](release-status.md). It is keyed to PK-Stack `v0.2.0` and
the version in `powers/pk-stack/plugin.json`; this file remains as a stable link
for older references. Pre-0.2 records live under
[`historical/pre-v0.2/`](historical/pre-v0.2/) and do not establish a current
acceptance verdict.
