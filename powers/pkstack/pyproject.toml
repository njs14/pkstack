[build-system]
requires = ["hatchling==1.32.0"]
build-backend = "hatchling.build"

[project]
name = "pkstack"
version = "0.5.4"
description = "PKStack Kiro-native verified development Power and project control CLI"
readme = "README.md"
requires-python = ">=3.11"
license = "Apache-2.0"
license-files = ["LICENSE", "NOTICE", "THIRD_PARTY_NOTICES.md"]
authors = [{ name = "PKStack contributors" }]
dependencies = [
  "cyclopts==4.23.2",
  "PyYAML>=6.0.2,<7",
  "markdown-it-py>=4,<5",
  "agent-client-protocol==0.12.1",
]

[dependency-groups]
dev = [
  "pytest>=8.4,<9",
  "pytest-cov>=6.2,<7",
  "ruff>=0.12,<0.14",
  "ty>=0.0.1a20,<1",
]

[tool.hatch.build.targets.wheel]
packages = ["src/pkstack"]

[tool.pytest.ini_options]
addopts = "-ra --strict-config --strict-markers --cov-branch"
testpaths = ["tests"]

[tool.coverage.run]
branch = true
source = ["pkstack"]

[tool.coverage.report]
show_missing = true
skip_covered = true
fail_under = 85

[tool.ruff]
target-version = "py311"
line-length = 100
# Generated snapshots are compared byte for byte by the packaging tests.
extend-exclude = ["tests/fixtures/generated"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "RUF"]

[tool.ruff.lint.per-file-ignores]
# The setup entrypoint runs on the interpreter that is already present, before
# uv provides one, so it must keep its own pre-3.11 version guard.
"skills/pkstack-setup/scripts/setup_pkstack.py" = ["UP036"]

[tool.ruff.format]
quote-style = "double"

[tool.ty.environment]
python-version = "3.11"
# `benchmarks/` and the demo example are standalone entrypoints rather than
# importable packages, so they need explicit first-party search paths.
root = ["src", "tests", "benchmarks", "examples/verified-goal-demo"]

[tool.ty.src]
include = ["src", "tests", "benchmarks", "examples", "skills/pkstack-setup/scripts"]
