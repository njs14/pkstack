---
name: principle-encode-lessons-in-structure
description: Replace a recurring correction with the strongest practical type, lint, CI, helper, or runtime mechanism and prove the observed mistake now fails automatically.
---

# Encode the lesson

Treat the request text that activated this skill as a recurring correction to make structural.

Apply [encode lessons in structure](../principles/references/catalog.md#encode-lessons-in-structure).
Name the repeated failure, choose the strongest practical enforcement layer, and add a regression
that demonstrates the old mistake is rejected. Keep prose only when judgment cannot be encoded.
Return the mechanism and the failing-then-passing evidence.
